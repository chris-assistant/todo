# Vanilla JS Todo App

A simple todo list app built with vanilla JavaScript that persists items in browser **localStorage**.

## Features
- Add, complete, and delete todos
- Persists across page reloads via `localStorage`
- Minimal, dependency-free HTML/CSS/JS

## Usage
1. Open `index.html` in your browser.
2. Add tasks using the input and **Add** button.
3. Click a task to toggle completion.
4. Click **Delete** to remove a task.

## File Structure
```
.
├── index.html
└── README.md
```

## Notes
- Data is stored under the `todos` key in `localStorage`.
- Works in any modern browser.
